<%- include('./partials/header') %>

    <% if(!user) { %>
        <h1>hello</h1>
        <%}else{%>
            <h1>hello 
                <%- user.displayName %>
            </h1>
            <%}%>
                <%- include('./partials/flash') %>

                    <% if(!user) { %>
                        <h1>wanna login!!</h1>
                        <%}else{%>
                            <h1>you're already loged in :)</h1>
                            <h1><a href="/logout">wanna logout ?........:)</a></h1>
                            <%}%>

                                <% if(!user) { %>
                                    <h1><a href="/login">login</a></h1>
                                    <h1><a href="/signup">signup</a></h1>
                                    <%}%>

                                        <%- include('./partials/footer') %>